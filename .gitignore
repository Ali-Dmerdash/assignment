node_modules
.DS_Store
dist
dist-ssr
*.local
.env

# Claude Code session/workspace dir
.claude/

# test coverage output
coverage/

# temp upload dir used by tests
backend/test/uploads-tmp/
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# uploaded CVs (local dev — written to ./uploads when UPLOAD_DIR is unset)
uploads/

# local production docker setup (not committed)
backend/prod/.env

# typescript
*.tsbuildinfo
